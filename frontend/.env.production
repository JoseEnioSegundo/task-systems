# Coloque este arquivo em frontend/.env.production
# Usado quando o Dockerfile roda `npm run build` para a imagem que vai pro cluster.
# Caminho relativo: o Traefik roteia /api para o backend no mesmo host/porta.
VITE_API_BASE_URL=/api/
