# Coloque este arquivo em task-systems/frontend/nginx.conf
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Suporte ao Vue Router (history mode)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
